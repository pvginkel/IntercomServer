/.vs
/*/bin
/*/obj
/tmp
# `dotnet publish ... -o publish` output; the Dockerfile's build context.
/publish
*.user
*.suo
launchSettings.json

# Real MCP server configuration may contain API tokens in headers.
mcpservers.json

# Persistent data (memories, etc.) written at runtime under DATA_DIR's default location.
/data
